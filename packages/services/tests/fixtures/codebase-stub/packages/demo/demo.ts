// Fixture file used by ai-tools.test.ts (search_codebase tool).
// Contains a unique token the test searches for: sopDocuments.

export const fixtureMarker = 'sopDocuments';
export const exportedThing = 'export-token';
// TODO: leave this comment so allow-list tests can match.
export function noop() {
  return null;
}
